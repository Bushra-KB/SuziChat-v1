/** Max voice message upload size (bytes). */
export const VOICE_UPLOAD_MAX_BYTES = 10 * 1024 * 1024;

/** Max voice message duration (milliseconds). */
export const VOICE_MAX_DURATION_MS = 5 * 60 * 1000;

/** Form field name used by multipart upload. */
export const VOICE_UPLOAD_FIELD = 'file';
