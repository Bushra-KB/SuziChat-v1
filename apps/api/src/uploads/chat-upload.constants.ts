/** Max chat file attachment size (bytes). */
export const CHAT_FILE_UPLOAD_MAX_BYTES = 25 * 1024 * 1024;

/** Form field name used by multipart upload. */
export const CHAT_FILE_UPLOAD_FIELD = 'file';
